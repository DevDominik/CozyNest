<Window x:Class="CozyNestAdmin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CozyNestAdmin" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="RoundedButton" TargetType="Button">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style for ListViewItem with left border when selected -->
        <Style x:Key="ListViewItemStyle" TargetType="ListViewItem">
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Padding" Value="10,5"/>
            <Style.Triggers>
                <!-- Apply left border ONLY when the item is selected -->
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="BorderBrush" Value="#007ACC"/>
                    <Setter Property="BorderThickness" Value="5,0,0,0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="15" BorderBrush="Black" BorderThickness="2">
        <Grid>
            <Grid Background="#007ACC" Height="40" VerticalAlignment="Top">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="40"/>
                    <ColumnDefinition Width="40"/>
                </Grid.ColumnDefinitions>

                <Label Content="Main Window" Foreground="White" FontSize="16" 
                       VerticalAlignment="Center" Padding="10,0,0,0"
                       MouseLeftButtonDown="Window_MouseLeftButtonDown"/>

                <Button Grid.Column="1" Content="━"
                        FontSize="18" Foreground="White"
                        Background="Transparent" BorderThickness="0"
                        ToolTip="Minimize"
                        Click="MinimizeWindow"/>

                <Button Grid.Column="2" Content="✖"
                        FontSize="18" Foreground="White"
                        Background="Transparent" BorderThickness="0"
                        ToolTip="Close"
                        Click="CloseWindow"/>
            </Grid>

            <Grid Margin="0,40,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Background="#2C3E50" Grid.Column="0" CornerRadius="0,0,0,0" Margin="0">
                    <StackPanel VerticalAlignment="Top" HorizontalAlignment="Center">
                        <TextBlock Margin="10px" Text="CozyNest" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="White" FontSize="32" FontWeight="Bold" FontFamily="MV Boli" />
                        <TextBlock Text="Management" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="White" FontSize="14" Margin="0,10,0,0" FontFamily="MV Boli"/>

                        <ListView Width="180" Margin="5" Background="Transparent" BorderBrush="Transparent" HorizontalAlignment="Center" ItemContainerStyle="{StaticResource ListViewItemStyle}">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Button Background="Transparent" BorderBrush="Transparent" HorizontalAlignment="Stretch" VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <materialDesign:PackIcon VerticalAlignment="Center" Foreground="White" Margin="5" Width="20" Height="20"/>
                                            <TextBlock Text="{Binding}" Foreground="White" VerticalAlignment="Center" Margin="10,0"/>
                                        </StackPanel>
                                    </Button>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                            <ListView.Items>
                                <ListViewItem>
                                    <ListViewItem.Content>
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="Home" VerticalAlignment="Center" Foreground="White" Margin="5" Width="20" Height="20"/>
                                            <TextBlock Text="Main" VerticalAlignment="Center" Foreground="White"/>
                                        </StackPanel>
                                    </ListViewItem.Content>
                                </ListViewItem>

                                <ListViewItem>
                                    <ListViewItem.Content>
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="AccountGroup" VerticalAlignment="Center" Foreground="White" Margin="5" Width="20" Height="20"/>
                                            <TextBlock Text="Rooms" VerticalAlignment="Center" Foreground="White"/>
                                        </StackPanel>
                                    </ListViewItem.Content>
                                </ListViewItem>

                                <ListViewItem>
                                    <ListViewItem.Content>
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="Account" VerticalAlignment="Center" Foreground="White" Margin="5" Width="20" Height="20"/>
                                            <TextBlock Text="Users" VerticalAlignment="Center" Foreground="White"/>
                                        </StackPanel>
                                    </ListViewItem.Content>
                                </ListViewItem>

                                <ListViewItem>
                                    <ListViewItem.Content>
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="Cog" VerticalAlignment="Center" Foreground="White" Margin="5" Width="20" Height="20"/>
                                            <TextBlock Text="Misc" VerticalAlignment="Center" Foreground="White"/>
                                        </StackPanel>
                                    </ListViewItem.Content>
                                </ListViewItem>
                            </ListView.Items>
                        </ListView>
                    </StackPanel>
                </Border>

                <Grid Grid.Column="1">
                    <Label Content="Main Content Goes Here" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </Grid>

            </Grid>
        </Grid>
    </Border>
</Window>
